<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MultiPlatformApplication.Controls"
             x:Class="MultiPlatformApplication.Controls.MessageOtherUser"
             Margin="0"
             Padding="0">
  <ContentView.Content>
        <Grid RowSpacing="0" ColumnSpacing="2" Padding="0" Margin="0">
            <Grid.ColumnDefinitions>
                <!-- SPACE -->
                <ColumnDefinition Width="4" />
                <!-- AVATAR -->
                <ColumnDefinition Width="40" />
                <!-- SPACE -->
                <ColumnDefinition Width="1" />
                <!-- BODY -->
                <ColumnDefinition Width="Auto" />
                <!-- SPACE -->
                <ColumnDefinition Width="4" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="1" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1" />
            </Grid.RowDefinitions>

            <!-- BODY -->
            <controls:MessageContent Grid.Row="1" 
                                     Grid.Column="3"
                                     VerticalOptions="Center"
                                     HorizontalOptions="Start"/>
        </Grid>
    </ContentView.Content>
</ContentView>