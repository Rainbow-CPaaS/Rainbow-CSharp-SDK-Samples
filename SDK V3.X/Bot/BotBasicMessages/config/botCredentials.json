{
  "_comment_botCredentials": "Settings used by the bot to connect to the Rainbow server: appId, appSecret, hostname, login, pwd",
  "botCredentials": {

    "_comment_serverConfig": "Define app id, app secret (which are different according hostName)",
    "serverConfig": {
      "appId": "TO DEFINE",
      "appSecret": "TO DEFINE",
      "hostname": "openrainbow.net"
    },

    "_comment_userConfig": "Define login and password to use to log to Rainbow server. Prefix to use for log purpose (must be unique if several bot are used in same time)",
    "userConfig": {
      "iniFolderPath": ".\\logs",
      "prefix": "USER1",
      "login": "TO DEFINE",
      "password": "TO DEFINE"
    }
  }
}
