{
  "applicationConfig": {
    "logsFolderPath": "./logs"
  },

  "serverConfig": {
    "appId": "TO DEFINE",
    "appSecret": "TO DEFINE",
    "hostname": "TO DEFINE"
  },

  "_comment_usersConfig": "ONE or SEVERAL Rainbow accounts can be defined, for each we detect if their connected to a Rainbow UCaaS application or not",
  "usersConfig": [
    {
      "prefix": "BOT1_",
      "login": "TO DEFINE",
      "password": "TO DEFINE",
      "autoLogin": true
    },
    {
      "prefix": "BOT2_",
      "login": "TO DEFINE",
      "password": "TO DEFINE",
      "autoLogin": true
    }
  ]
}
