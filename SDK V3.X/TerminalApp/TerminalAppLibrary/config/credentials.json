{
  "_comment_credentials": "Settings used by the bot to connect to the Rainbow server: appId, appSecret, hostname, login, pwd",
  "credentials": {

    "_comment_serverConfig": "Define app id, app secret (which are different according hostName)",
    "serverConfig": {
      "appId": "TO DEFINE",
      "appSecret": "TO DEFINE",
      "hostname": "TO DEFINE"
    },

    "_comment_userConfig": "Define one or several login and password to use to log to Rainbow server. Prefix to use for log purpose (must be unique if several bot are used in same time)",
    "usersConfig": [
      {
        "iniFolderPath": ".\\logs",
        "prefix": "USER1",
        "login": "TO DEFINE",
        "password": "TO DEFINE",
        "autoLogin": true
      },
      {
        "iniFolderPath": ".\\logs",
        "prefix": "USER2",
        "login": "TO DEFINE",
        "password": "TO DEFINE",
        "autoLogin": true
      }
    ]
  }
}
